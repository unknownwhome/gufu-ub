#meta_developer: @unknwhome

from ..core import loader, utils

class PYTONCHIK(loader.Module):
    @loader.command()
    async def pornocmd(self, message):
        """dev by @unknwhome"""
        await utils.answer(message, "Сосать америка")
